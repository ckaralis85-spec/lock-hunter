# Lock Hunter runtime dependencies.
# tkinter is part of the Python standard library (no pip install needed),
# but on some Linux distros it ships as a separate OS package (python3-tk).
requests>=2.31
pillow>=10.0
openpyxl>=3.1
# Strongly recommended: gives EVERY scraping probe (eBay, the ~46
# marketplaces, Facebook, listing verification) a real Chrome TLS
# fingerprint. Many marketplaces bot-wall plain-requests TLS no matter the
# headers; with curl_cffi they serve real results. The app still runs
# without it — searches just get blocked more often. (If it complicates the
# PyInstaller build on your machine, remove it.)
curl_cffi>=0.7
